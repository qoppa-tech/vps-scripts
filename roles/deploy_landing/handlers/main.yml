#SPDX-License-Identifier: MIT-0
---
# handlers file for deploy_landing

- name: Restart landing
  ansible.builtin.shell: |
    cd {{ landing_app_dir }}
    docker compose down
    docker compose up -d
  become: true
  become_user: "{{ deploy_user }}"
