services:
  landing:
    image: {{ landing_image }}
    container_name: landing
    restart: unless-stopped
    ports:
      - "{{ landing_port }}:80"
    environment:
      - NODE_ENV=production
    networks:
      - parlor-network

networks:
  parlor-network:
    external: true
